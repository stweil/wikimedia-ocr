image: debian:bookworm

variables:
  APP_ENV: test

ocr-test:
  stage: test
  script:
    - |
      apt update -q
      apt install -y curl zip php php-zip php-imagick php-ast php-bcmath php-curl php-gd php-dom php-intl \
        php-simplexml php-xml php-mbstring tesseract-ocr-all nodejs npm jq
      curl -sS https://getcomposer.org/installer | php
      php composer.phar install
      npm ci
      php composer.phar test
      npm run test
      npm run build
      git status
      git status | grep "nothing added to commit"
  artifacts:
    when: on_failure
    paths:
      - ./var/log/test.log
  tags:
    - wmcs
